import "@hotwired/turbo-rails"
import "controllers"
import "bootstrap/dist/js/bootstrap.bundle"

document.addEventListener("turbo:load", () => {
  // No manual dropdown init needed — Bootstrap handles this automatically
})
